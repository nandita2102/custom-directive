import { UserDataDirective } from './user-data.directive';

describe('UserDataDirective', () => {
  it('should create an instance', () => {
    const directive = new UserDataDirective();
    expect(directive).toBeTruthy();
  });
});
